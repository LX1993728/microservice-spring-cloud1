spring:
  application:
    name: microservice-gateway-zuul-redis-session
  redis:
    host: localhost
    database: 0

server:
  port: 8040

eureka:
  client:
    service-url:
      defaultZone: http://liuxun:123456@localhost:8761/eureka
  instance:
    prefer-ip-address: true
zuul:
  sensitive-headers: Expires
